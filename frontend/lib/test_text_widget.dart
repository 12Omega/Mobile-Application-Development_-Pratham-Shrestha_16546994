import 'package:flutter/material.dart';

class TestTextWidget extends StatelessWidget {
  const TestTextWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Test Text Widget');
  }
}
